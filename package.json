{
  "name": "olive",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "wrangler": "^3.15.0"
  }
}
