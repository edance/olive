{
  "name": "olive",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "wrangler": "^3.15.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vite": "^4.5.0"
  }
}
